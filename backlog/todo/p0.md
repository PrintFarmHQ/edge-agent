# PrintFarm Edge — Bambu Cloud Integration Plan (4 Phases, TDD-first)

## Goal
Add production-grade Bambu support to the Go edge agent while preserving the existing control-plane contract.

## Delivery Sequence (Requested)
1. Phase 1: Authenticate to Bambu Cloud (including MFA when required)
2. Phase 2: Retrieve cloud printer list and build local printer state in edge-agent
3. Phase 3: Send print command to any available printer
4. Phase 4: Implement remaining Bambu commands and full lifecycle behavior

## Locked Decisions (Agreed)
- Bambu mode is cloud-first.
- Bambu discovery is cloud enumeration primary, with LAN/Bambu Connect fallback when cloud auth/API is unavailable.
- Control-plane contract stays unchanged: keep `bindings`, `desired-state`, and `state-push` model.
- Existing non-Bambu flags and existing non-Bambu functionalities are strict compatibility constraints and must not change in any phase.
- Adoption ownership is in SaaS, not edge.
- Edge does not keep adopted/not-adopted truth for printers.
- Edge runs as a long-lived process started via flags (no standalone login workflow).
- Bambu startup auth uses only `--bambu` as runtime flag.
- Bambu auth flow is token-first:
  - if stored token is valid, reuse it with no prompts;
  - if expired and refresh token is available, refresh token first;
  - otherwise run interactive login (username, password, MFA when required).
- Password input must be treated as secret and not echoed in terminal mode.
- MFA is collected interactively after login challenge (no pre-supplied MFA flags).
- MFA delivery is cloud-driven from the login challenge flow (avoid explicit separate code-send API calls).
- Passwords are never persisted; token material is persisted.
- Bambu auth failure at startup is fatal for process startup (non-zero exit).
- Local state path for this work: `~/.printfarmhq/`.
- Existing ecosystem packages can be used (including sidecar approach if required).
- No reverse-engineering activities are part of this plan.

## Non-goals
- AMS inventory management
- Camera streaming
- Slicing in edge-agent
- Broad control-plane schema redesign

---

## Runtime Invocation Contract
1. Keep edge-agent as a long-lived process launched directly with flags.
2. Preserve operator model:
   - `./edge-agent --api-key=<saas_key> --klipper`
   - `./edge-agent --api-key=<saas_key> --klipper --bambu`
3. Preserve existing non-Bambu flags with identical names, parsing rules, and behavior.
4. When `--bambu` is enabled:
   - check stored token in `~/.printfarmhq/bambu/credentials.json`;
   - reuse if valid;
   - refresh if expired and refresh token is available;
   - otherwise prompt interactively for username and password.
5. Do not add standalone `bambu login/logout/status` workflows.
6. MFA handling is startup-driven and interactive:
   - if cloud requires MFA, agent blocks on console and prompts for MFA code;
   - wrong MFA code or missing MFA code causes startup failure with non-zero exit.

---

## Current Reality (Repo Baseline)
- Existing implementation is monolithic in `cmd/edge-agent/main.go`.
- Bambu support exists for discovery/snapshot via Bambu Connect bridge.
- Bambu action execution currently rejects lifecycle operations.
- Desired-state reconciliation and state-push loops are already in place and remain the integration spine.

---

## Foundational Workstream (Cross-phase)
This workstream is executed first and then iteratively extended during Phases 1-4.

### Target package layout
- `cmd/edge-agent/main.go` (runtime bootstrap entrypoint)
- `internal/app/`
- `internal/config/`
- `internal/store/`
- `internal/controlplane/`
- `internal/discovery/`
- `internal/adapters/moonraker/`
- `internal/adapters/bambu/`
- `internal/bambu/auth/`
- `internal/bambu/cloud/`

### Acceptance
- Behavior parity for non-Bambu paths.
- Control-plane payload parity captured in tests.
- Existing flags and existing non-Bambu functionality remain unchanged.
- `go test ./...` green.

---

## Phase 1 — Cloud Authentication (with MFA)

### Scope
- Implement Bambu cloud auth negotiation at startup for long-lived agent process.
- Persist token metadata and refresh material in local secure store.
- Support MFA challenge flow when cloud requires it.

### Inputs
- Required runtime trigger:
  - `--bambu`
- Interactive inputs when token is missing/invalid:
  - username
  - password (hidden/no-echo in terminal mode)
  - MFA code when challenged by cloud

### Persistence
- `~/.printfarmhq/bambu/credentials.json` (`0600`)
- fields:
  - `access_token`
  - `refresh_token` (if available)
  - `expires_at`
  - masked account metadata

### TDD backlog
1. [TEST] `TestParseRuntimeFlagsBambuOnly`
2. [TEST] `TestParseRuntimeFlagsRejectsRemovedBambuCredentialFlags`
3. [TEST] `TestBambuAuthStartupUsesStoredValidToken`
4. [TEST] `TestBambuAuthStartupRefreshesExpiredToken`
5. [TEST] `TestBambuAuthRefreshFailureFallsBackToInteractiveLogin`
6. [TEST] `TestBambuAuthMFAWithInteractiveCodeSucceeds`
7. [TEST] `TestBambuAuthMFAWithEmptyInteractiveCodeFails`
8. [TEST] `TestBambuAuthMFAWrongCodeFails`
9. [TEST] `TestBambuAuthInteractiveLoginRequiresInteractiveConsole`
10. [TEST] `TestTokenStore_FilePermissions0600`
11. [CODE] token-first auth manager + interactive MFA negotiation + secure token store

### Exit criteria
- Agent with `--bambu` authenticates and stores token material in `~/.printfarmhq`.
- MFA-required accounts are supported through interactive startup prompts.
- Bambu init failures terminate startup with explicit error and non-zero exit.
- No password persistence on disk.
- Existing non-Bambu flags and non-Bambu behavior are unchanged.

---

## Phase 2 — Cloud Printer List + Local State

### Scope
- Retrieve printers from cloud API using Phase 1 auth.
- Build/maintain local discovered-printer state.
- Keep LAN/Bambu Connect discovery as fallback when cloud is unavailable.
- Report inventory through existing control-plane pipeline.

### Local persistence
- `~/.printfarmhq/bambu/discovered_printers.json`
- `~/.printfarmhq/bambu/cache.json`

`discovered_printers.json` minimum fields:
- `identity` (`serial`/`device_id`)
- `serial`
- `device_id`
- `model`
- `name`
- `endpoint_hint`
- `source` (`cloud` or `lan_fallback`)
- `last_seen_at`

### Rules
- Primary source: cloud printer enumeration.
- Fallback source: Bambu Connect/LAN when cloud auth/API path is unavailable.
- Merge/dedupe key: `serial`, then `device_id`.
- No edge-side adoption command.

### TDD backlog
1. [TEST] `TestBambuDiscovery_CloudPrimaryEnumeration`
2. [TEST] `TestBambuDiscovery_FallbackToLANWhenCloudUnavailable`
3. [TEST] `TestBambuDiscovery_DedupBySerialThenDeviceID`
4. [TEST] `TestDiscoveredPrintersStore_RoundTrip`
5. [TEST] `TestInventoryReport_UsesMergedBambuState`
6. [CODE] cloud list client + merge/fallback + persistence + inventory report integration

### Exit criteria
- With valid cloud auth, Bambu local state is cloud-derived.
- With cloud unavailable, state falls back to LAN/Bambu Connect.
- Restart preserves discovered-printer state.
- Existing flags and existing non-Bambu behavior are unchanged.

---

## Phase 3 — Print Command (Start Print)

### Scope
- Route Bambu `print` action from existing desired-state reconciliation to cloud execution path.
- Support artifact download, cloud upload/submit, and print start.
- Keep control-plane contract unchanged.

### Print execution path
1. Desired-state indicates print for a Bambu binding.
2. Download artifact via existing artifact mechanism.
3. Submit/upload/start via Bambu cloud adapter.
4. Push current-state updates via existing state-push loop.

### TDD backlog
1. [TEST] `TestBambuStartPrint_RequiresValidAuth`
2. [TEST] `TestDesiredStatePrintAction_RoutesToBambuAdapter`
3. [TEST] `TestArtifactDownloadAndCleanup_BambuPath`
4. [TEST] `TestBambuCloudSubmitAndStart_Succeeds`
5. [TEST] `TestBambuCloudStartFailure_DeadLetterAndRetry`
6. [CODE] Bambu start-print action implementation in adapter

### Exit criteria
- Print start works for cloud-authenticated Bambu printers.
- Failures are surfaced through existing retry/dead-letter behavior.
- State updates continue through existing schema and endpoints.
- Existing flags and existing non-Bambu behavior are unchanged.

---

## Phase 4 — Remaining Commands + Full Lifecycle

### Scope
- Implement pause/resume/cancel actions.
- Implement progress/telemetry updates and terminal state handling.
- Implement refresh/reconnect/recovery behavior.

### Commands and lifecycle
- Implement desired-state action mapping for Bambu:
  - `pause`
  - `resume`
  - `stop` (mapped to cancel)
- Maintain normalized state model parity:
  - printer: `idle`, `queued`, `printing`, `paused`, `error`
  - job: `pending`, `printing`, `completed`, `failed`, `canceled`

### Resilience
- Token refresh on expiry.
- Reconnect/status stream recovery after transient failures.
- Persistent auth failures mark Bambu path as auth-required/manual-intervention without crashing agent.

### TDD backlog
1. [TEST] `TestBambuPauseResumeCancel_RoutesAndPayloads`
2. [TEST] `TestBambuProgressTranslation_ToCurrentState`
3. [TEST] `TestBambuTerminalStateTranslation`
4. [TEST] `TestTelemetry_ThrottledByConfigInterval`
5. [TEST] `TestAuthExpiry_RefreshAndRecover`
6. [TEST] `TestPersistentAuthFailure_DegradesBambuOnly`
7. [CODE] full remaining action + telemetry + recovery implementation

### Exit criteria
- Pause/resume/cancel work reliably.
- Progress + terminal states are pushed through existing state contract.
- Transient failures recover; persistent auth errors degrade Bambu only.
- Existing flags and existing non-Bambu behavior are unchanged.

---

## Config Additions
`~/.printfarmhq/config.json` additions:

```json
{
  "bambu": {
    "enabled": true,
    "mode": "cloud_primary",
    "fallback": {
      "lan_bambu_connect_enabled": true,
      "bambu_connect_uri": ""
    },
    "cloud": {
      "endpoint_overrides": {
        "auth_base_url": "",
        "api_base_url": "",
        "mqtt_host": "",
        "mqtt_port": 0
      }
    },
    "telemetry": {
      "publish_interval_seconds": 5
    }
  }
}
```

Notes:
- Password input must come from interactive startup prompt (no persisted config, no persisted password).
- MFA codes are one-time interactive startup inputs and must not be persisted.

---

## QA and CI Requirements
- Hermetic tests only (no real Bambu cloud in CI).
- Fakes/mocks for cloud and fallback providers.
- Golden tests for state/control-plane payload compatibility.
- Compatibility regression suite for all existing flags and existing non-Bambu workflows must pass in every phase.
- `go test ./...` must remain green at each phase.

---

## Risks and Mitigations
- Cloud API instability: isolate via `cloud` interfaces + endpoint overrides.
- MFA friction in headless/non-interactive environments: fail fast with explicit interactive-console-required error.
- Sidecar complexity (if used): strict health check, timeouts, typed error mapping.
- Refactor regressions: characterization tests and payload parity tests before behavior changes.
